import 'package:flutter/material.dart';

void showConfirmOrderNotification(BuildContext context, String message) {
  // SnackBar removed per user request. Function retained as a no-op so
  // existing call sites don't need changes.
  return;
}
